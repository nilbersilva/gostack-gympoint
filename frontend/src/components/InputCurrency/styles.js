import styled from 'styled-components';
import InputCurrencyControl from 'react-currency-input';

export const InputCurControl = styled(InputCurrencyControl)`
  text-align: ${props => props.textAlign};
`;
